{% extends beelab_reputation_layout %}

{% block body %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <p>
            <button type="submit" class="btn btn-default">OK</button>
        </p>
    {{ form_end(form) }}

{% endblock %}
